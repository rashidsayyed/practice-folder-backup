/* Read input from STDIN. Print your output to STDOUT*/
#include<iostream>
using namespace std;
int main(int argc, char *a[])
{
    int nt;
    cin >> nt;
    int NumberOfGifts[nt];
    int NumberOfTotalGifts[nt];
    for(int i=0;i<=nt;i++){
        cin >> NumberOfGifts[i];
        cin >> NumberOfTotalGifts[i];
        for(int j=0;j)
        cin >> pricesOfGifts[gt];
    }
}